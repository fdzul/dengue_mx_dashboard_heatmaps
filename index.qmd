---
title: "Programa de Prevención y Control del Dengue | 2024"
format: 
  dashboard:
    scrolling: true
    logo: https://www.gob.mx/cms/uploads/action_program/main_image/26942/post_post_portadavectores.gif
    nav-buttons: [twitter, github, facebook]
theme: custom.scss
---

```{r setup}
library(sf)
mapview::mapviewOptions(default = TRUE,
                        basemaps.color.shuffle = FALSE)
load("/Users/fdzul/Library/CloudStorage/OneDrive-Personal/proyects/geocoding_mex/2024/8.RData/denmex_2024.RData")
```

# [**Mapas de Calor**]{style="font-size:8pt"}

::: panel-tabset

### [**O6 Colima**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Área Metropolitana de Colima
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r colima_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Colima", 
                                "Ciudad de Villa Alvarez"),
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r colima_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Colima", 
                                "Ciudad de Villa Alvarez"),
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r colima_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Colima", 
                                "Ciudad de Villa Alvarez"),
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Tecomán
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r tecoman_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tecomán",
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r tecoman_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tecomán",
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r tecoman_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tecoman",
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Manzanillo
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r manzanillo_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Manzanillo",
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r manzanillo_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Manzanillo",
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r manzanillo_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Manzanillo",
                   cve_edo = "06",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


### [**11 Guanajuato**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
León
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r leon_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "León de Aldama",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r leon_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Leon de Aldama",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r leon_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "León de Aldama",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Guanajuato
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r guanajuato_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guanajuato",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r guanajuato_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guanajuato",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r guanajuato_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guanajuato",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Celaya
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r celaya_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Celaya",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r celaya_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Celaya",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r celaya_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Celaya",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Acambaro
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r acambaro_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Acambaro",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r acambaro_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Acambaro",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r acambaro_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Acambaro",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Irapuato
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r irapuato_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Irapuato",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r irapuato_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Irapuato",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r irapuato_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Irapuato",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Silao
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r silao_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Silao de la Victoria",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r silao_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Silao de la Victoria",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r silao_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Silao de la Victoria",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Salamanca
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r salamanca_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Salamanca",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r salamanca_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Salamanca",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r salamanca_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Salamanca",
                   cve_edo = "11",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>

### [**14 Jalisco**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 20px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Área Metropolitana de Guadalajara
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r guadalajara_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Guadalajara", "Tlaquepaque",
                                "Zapopan", "Tonalá"),
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r guadalajara_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Guadalajara", "Tlaquepaque",
                                "Zapopan", "Tonalá"),
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r guadalajara_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Guadalajara", "Tlaquepaque",
                                "Zapopan", "Tonalá"),
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::
</body>
</html>



<html>
<head>

</head>
<body>
<h4>
Cocula
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r cocula_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cocula",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cocula_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cocula",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r cocula_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cocula",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>


<html>
<head>

</head>
<body>
<h4>
Tequila
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r tequila_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tequila",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r tequila_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tequila",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r tequila_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Tequila",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>



<html>
<head>

</head>
<body>
<h4>
Autlán de Navarro
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r autlan_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Autlán de Navarro",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r autlan_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Autlán de Navarro",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r autlan_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Autlán de Navarro",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>



<html>
<head>

</head>
<body>
<h4>
Cihuatlán
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r cihuatlan_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cihuatlán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cihuatlan_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cihuatlán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r cihuatlan_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cihuatlán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>


<html>
<head>

</head>
<body>
<h4>
Ciudad Guzmán
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r cd_guzman_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ciudad Guzmán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cd_guzman_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ciudad Guzmán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r cd_guzman_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ciudad Guzmán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>


<html>
<head>

</head>
<body>
<h4>
Chapala
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r chapala_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Chapala",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r chapala_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Chapala",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r chapala_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Chapala",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>


<html>
<head>

</head>
<body>
<h4>
La Barca
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r barca_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "La Barca",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r barco_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "La Barca",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r barca_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "La Barca",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>


<html>
<head>

</head>
<body>
<h4>
Ocotlán
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r ocotlan_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ocotlán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r ocotlan_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ocotlan",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r ocotlan_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ocotlán",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>

<html>
<head>

</head>
<body>
<h4>
Puerto Vallarta
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r vallarta_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Puerto Vallarta",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r vallarta_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Puerto Vallarta",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r vallarta_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Puerto Vallarta",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>


<html>
<head>

</head>
<body>
<h4>
Pihuamo
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r pihuamo_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Pihuamo",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r pihuamo_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Pihuamo",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r pihuamo_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Pihuamo",
                   cve_edo = "14",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1,2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

:::

</body>
</html>

### [**17 Morelos**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Cuernavaca
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r cuernavaca_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuernavaca",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cuernavaca_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuernavaca",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r cuernavaca_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuernavaca",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Juitepec
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::

::: grid-item1
```{r juitepec_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jiutepec",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```

:::

::: grid-item1
```{r jiutepec_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Jiutepec",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::

::: grid-item1
```{r juitepec_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jiutepec",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```

:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Temixco
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::

::: grid-item1
```{r temixco_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Temixco",
                    cve_edo = "17",
                    geocoded_datasets = z,
                    zoom = 13,
                    status_caso = c(1, 2),
                    week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                    alpha = 0.2,
                    static = TRUE,
                    map_type = "satellite")
```
:::

::: grid-item1

```{r temixco_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Temixco",
                    cve_edo = "17",
                    geocoded_datasets = z,
                    zoom = 13,
                    status_caso = c(1, 2),
                    week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                    alpha = 0.2,
                    static = TRUE,
                    map_type = "satellite")
```
:::

::: grid-item1

```{r temixco_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Temixco",
                    cve_edo = "17",
                    geocoded_datasets = z,
                    zoom = 13,
                    status_caso = c(1, 2),
                    week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                    alpha = 0.2,
                    static = TRUE,
                    map_type = "satellite")
```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Cuautla
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::

::: grid-item1
```{r cuautla_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuautla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cuautla_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuautla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cuautla_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cuautla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Jojutla
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::

::: grid-item1
```{r jojutla_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jojutla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 16,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

::: grid-item1
```{r jojutla_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jojutla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 16,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::

::: grid-item1
```{r jojutla_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
densnv::mp_heatmap(locality = "Jojutla",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 16,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


:::

</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Emiliano Zapata
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::

::: grid-item1
```{r emiliano_zapata_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Emiliano Zapata",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r emiliano_zapata_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Emiliano Zapata",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r emiliano_zapata_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Emiliano Zapata",
                   cve_edo = "17",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


:::

</body>
</html>


### [**18 Nayarit**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto auto;
  padding: 5px;
}

.grid-item {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align-last: center;
  text-align: center;
}
</style>
```
</head>
<body>
<h1>
</h1>
<p>
</p>

::: grid-container

::: grid-item
Localidad 
:::

::: grid-item
Acumulado
:::

::: grid-item
Ultimo Mes
:::

::: grid-item
Ultimas 2 Semanas
:::

::: grid-item
Tepic
:::

::: grid-item
```{r tepic_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
load("/Users/fdzul/Library/CloudStorage/OneDrive-Personal/proyects/geocoding_mex/2024/8.RData/denmex_2024.RData")

########
densnv::mp_heatmap(locality = c("Tepic", "Xalisco"),
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 13,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r tepic_ultimo_mes}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####

########
densnv::mp_heatmap(locality = c("Tepic", "Xalisco"),
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 13,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r tepic_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####

########
densnv::mp_heatmap(locality = c("Tepic", "Xalisco"),
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 13,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
Bahía de Banderas
:::


::: grid-item
```{r bahia_acumulado}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Bahia de Banderas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 16,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r bahia_ultimo_mes, error=FALSE}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Bahia de Banderas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 16,
                        status_caso = c( 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, 
                                   to = lubridate::epiweek(Sys.Date()), 
                                   by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
```{r bahia_ultimas_dos_semanas}
#| echo: false
#| output: true
# Step 1. load the dengue dataset geocoded ####
########
densnv::mp_heatmap(locality = "Bahia de Banderas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 16,
                        status_caso = c( 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```

:::

::: grid-item
San Blas
:::


::: grid-item

```{r San_blas_acumulado}
#| echo: false
#| output: true

########
densnv::mp_heatmap(locality = "San Blas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```
:::


::: grid-item

```{r San_blas_ultimo_mes}
#| echo: false
#| output: true

########
densnv::mp_heatmap(locality = "San Blas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```
:::


::: grid-item

```{r San_blas_ultimas_dos_semanas}
#| echo: false
#| output: true

########
densnv::mp_heatmap(locality = "San Blas",
                        cve_edo = "18",
                        geocoded_datasets = z,
                        zoom = 15,
                        status_caso = c(1, 2),
                        week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                        alpha = 0.2,
                        static = TRUE,
                        map_type = "satellite")
```
:::



:::

</body>
</html>


### [**19 Nuevo León**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Área Metropolitana de Monterrey
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r mty_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Ciudad General Escobedo", "Ciudad Apodaca",
                                "Guadalupe", "Monterrey", "Ciudad Santa Catarina",
                                "San Pedro Garza García", "Ciudad Benito Juárez",
                                "San Nicolás de los Garza", "Montebello",
                                "Ciudad Sabinas"),
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 11,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r mty_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Ciudad General Escobedo", "Ciudad Apodaca",
                                "Guadalupe", "Monterrey", "Ciudad Santa Catarina",
                                "San Pedro Garza García", "Ciudad Benito Juárez",
                                "San Nicolás de los Garza", "Montebello",
                                "Ciudad Sabinas"),
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 11,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r mty_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = c("Ciudad General Escobedo", "Ciudad Apodaca",
                                "Guadalupe", "Monterrey", "Ciudad Santa Catarina",
                                "San Pedro Garza García", "Ciudad Benito Juárez",
                                "San Nicolás de los Garza", "Montebello",
                                "Ciudad Sabinas"),
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 11,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Cadereyta Jimenez
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r cadereyta_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cadereyta Jimenez",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cadereyta_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality =  "Cadereyta Jimenez",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r cadereyta_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Cadereyta Jimenez",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Ciudad de Allende
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r cd_allende_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ciudad de Allende",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r cd_allende_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ciudad de Allende",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r cd_allende_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Ciudad de Allende",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Linares
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r linares_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Linares",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r linares_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Linares",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r linares_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Linares",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Montemorelos
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r montemorelos_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Montemorelos",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r montemorelos_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Montemorelos",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r montemorelos_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Montemorelos",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>



<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Santiago
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r santiago_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Santiago",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r santiago_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Santiago",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r santiago_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Santiago",
                   cve_edo = "19",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


### [**25 Sinaloa**]{style="color:#990000"}

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Culiacán
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r culiacan_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Culiacan de Rosales",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r culiacan_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Culiacan de Rosales",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r culiacan_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Culiacan de Rosales",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Mazatlan
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r mazatlan_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Mazatlan",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r mazatlan_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Mazatlan",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::


::: grid-item1
```{r mazatlan_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Mazatlan",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 12,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>




<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Escuinapa
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r escuinapa_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Escuinapa de Hidalgo",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r escuinapa_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Escuinapa de Hidalgo",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r escuinapa_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Escuinapa de Hidalgo",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 15,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Guamuchil
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r guamuchil_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guamuchil",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r guamuchil_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guamuchil",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r guamuchil_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guamuchil",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 14,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Guasave
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r guasave_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guasave",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r guasave_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Guasave",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r guasave_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "guasave",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


</body>
</html>

<html>
<head>
```{=html}
<style>
.grid-container1 {
  display: grid;
  grid-template-columns: auto auto auto;
  padding: 5px;
}

.grid-item1 {
  background-color: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(0, 0, 0, 0.8);
  padding: 5px;
  font-size: 15px;
  text-align: center;
}
</style>
```
</head>
<body>
<h4>
Los Mochis
</h4>
<p>
</p>

::: grid-container1

::: grid-item1
Acumulado
:::

::: grid-item1
Último Mes
:::

::: grid-item1
Últimas 2 Semanas
:::


::: grid-item1
```{r mochis_acumulado}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Los Mochis",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = 1, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r mochis_ultimo_mes}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Los Mochis",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-4, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")
```
:::

::: grid-item1
```{r mochis_ultimas_dos_semanas}
#| echo: false
#| output: true
densnv::mp_heatmap(locality = "Los Mochis",
                   cve_edo = "25",
                   geocoded_datasets = z,
                   zoom = 13,
                   status_caso = c(1, 2),
                   week = seq(from = lubridate::epiweek(Sys.Date())-2, to = lubridate::epiweek(Sys.Date()), by = 1),
                   alpha = 0.2,
                   static = TRUE,
                   map_type = "satellite")

```
:::


:::

</body>
</html>


:::